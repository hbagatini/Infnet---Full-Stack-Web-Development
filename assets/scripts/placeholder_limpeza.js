window.addEventListener("load", ()=>{
    //all images
    const allImages = document.querySelectorAll(".images-limp")
    const allPlaceholdersImg = document.querySelectorAll(".img-placeholder")
    for (let i = 0; i < allImages.length; i++) {
        allImages[i].classList.remove("d-none");
        allPlaceholdersImg[i].classList.add("d-none");
        }
})


window
const img_limp = document.querySelectorAll(".images-limp")
